class Rol {
    private rol: string;
    public constructor(rol: string){
     this.rol = rol;
     }
    public setRol(rol: string){
     this.rol = rol;
     }
    public getRol(){
     return this.rol;
     }
}